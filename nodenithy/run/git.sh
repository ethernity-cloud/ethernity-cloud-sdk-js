#!/bin/sh

# apk add git

# ls -la

# echo "${REGISTRY_PATH}"
# cd ${REGISTRY_PATH}

# ls -la

# #commit add all to git
# ls -la ${CI_PROJECT_DIR}/etny-hash-packages/etny-nodenithy/v3/
# echo "Adding changed files to GIT: IPFS_HASH, IPFS_DOCKER_COMPOSE_HASH, CERTIFICATE..."
# git -C ${CI_PROJECT_DIR}/etny-hash-packages add etny-nodenithy/v3/.
# echo "Committed all files"
# git -C ${CI_PROJECT_DIR}/etny-hash-packages commit -m 'Adding changed files to GIT: IPFS_HASH, IPFS_DOCKER_COMPOSE_HASH, CERTIFICATE'
# echo "Pushed all files"
# git -C ${CI_PROJECT_DIR}/etny-hash-packages push
# echo "Added and committed all files"
# cd -

